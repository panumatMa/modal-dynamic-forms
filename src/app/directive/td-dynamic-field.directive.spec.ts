import { TdDynamicFieldDirective } from './td-dynamic-field.directive';

describe('TdDynamicFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new TdDynamicFieldDirective();
    expect(directive).toBeTruthy();
  });
});
